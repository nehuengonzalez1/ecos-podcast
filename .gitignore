node_modules
.next
out
dist
.env
.env.local
.env*.local
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.vercel

.vercel
.env*
# La plantilla de variables si tiene que viajar: es lo que le dice a quien
# clone el repo que necesita configurar. No lleva ningun valor real.
!.env.example

# Cache incremental de TypeScript, se regenera en cada build.
tsconfig.tsbuildinfo
